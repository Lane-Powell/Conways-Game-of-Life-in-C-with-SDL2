An implementation of Conway's Game of Life, made for educational purposes.

life.c is the main source file.
conway.c contains functions related to the Game of Life grid, including the game logic itself.
conjuror.c contains SDL functions.

Only has one built-in pattern (see function create_alternating_array).


To compile, run make in the command line.

To run, enter ./test in the command line.